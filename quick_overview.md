# [나도 Agent] 인터뷰만으로 제안서·개발설계서를 만드는 `PlanForge`

안녕하세요. 업무에서 반복적으로 발생하는 **기획자료 정리, 제안서 작성, 개발설계서 작성, 문서 검수** 업무를 보다 효율적으로 수행하기 위해 개발한 `PlanForge`를 소개합니다.

`PlanForge`는 사용자가 브라우저에서 Agent와 인터뷰를 진행하면, 대화 내용을 바탕으로 기획 문서를 정리하고 제안서·개발설계서 등의 산출물까지 자동으로 생성해 주는 웹 서비스입니다.

단순히 “문서를 작성해 달라”고 요청하는 방식이 아니라, Agent가 필요한 내용을 질문하고 사용자의 답변에서 확정된 사실을 축적한 뒤 문서로 변환한다는 점이 핵심입니다.

---

## 1. 활용한 Agent명

**PlanForge**

- 업무 AI 분야
- 제안서 및 개발설계서 작성 지원 Agent
- 브라우저 기반 웹 서비스
- GitHub 저장소: [https://github.com/e-kyu/makeReport](https://github.com/e-kyu/makeReport)

---

## 2. 개발하게 된 배경

제안서나 개발설계서를 작성할 때 다음과 같은 작업이 반복적으로 발생합니다.

- 여러 자료를 읽고 핵심 내용을 정리하는 작업
- 누락된 요구사항이나 확인이 필요한 내용을 다시 질문하는 작업
- 확정된 내용을 기획서 형식으로 재구성하는 작업
- 동일한 내용을 PPT, 문서, HTML 등 여러 형식으로 변환하는 작업
- 문서에 포함된 수치와 사실관계를 다시 검수하는 작업
- 문서를 수정할 때 이전 버전과 변경사항을 비교하는 작업

기존에는 각각의 작업을 수작업으로 진행하거나, AI에게 작성과 수정을 여러 차례 요청해야 했습니다. 이 과정에서 다음과 같은 문제가 있었습니다.

- 필요한 정보가 누락된 상태로 문서가 작성될 수 있음
- AI가 근거 없는 내용을 추정할 가능성이 있음
- 문서 형식에 따라 내용을 반복해서 재작성해야 함
- 문서 수정 과정에서 이전 버전과의 차이를 추적하기 어려움
- 제안서와 개발설계서의 수치나 사실관계가 달라질 수 있음

이러한 문제를 줄이고, **인터뷰 → 사실 확정 → 기획 문서 작성 → 산출물 생성 → 검수**의 과정을 하나의 흐름으로 연결하기 위해 `PlanForge`를 개발했습니다.

---

## 3. 주요 기능

### ① Agent 인터뷰를 통한 요구사항 정리

사용자가 처음부터 완성된 문서를 작성할 필요 없이, Agent와 대화하며 필요한 내용을 정리할 수 있습니다.

- 기존 자료 업로드
- Agent의 예상 내용 및 질문 확인
- 자유로운 답변 또는 선택지 카드로 응답
- 라운드별 확정 사실 확인
- 핵심 메시지 승인
- 필요한 정보가 모두 정리되면 인터뷰 종료

Agent가 처음부터 내용을 임의로 작성하는 것이 아니라, 사용자의 답변을 통해 확정된 사실을 축적하도록 구성했습니다.

---

### ② 확정된 사실을 기반으로 한 Plan 작성

인터뷰 결과는 하나의 기획 문서인 `plan`으로 정리됩니다.

- 인터뷰에서 확인된 내용 반영
- 근거가 부족한 수치는 `(미확정)`으로 표시
- 사용자가 Plan을 직접 확인하고 수정 가능
- 승인된 Plan만 다음 단계로 진행
- 이전 버전과의 Diff 확인 가능

문서의 원본은 Plan으로 관리하며, 이후 생성되는 모든 산출물은 승인된 Plan을 기준으로 만들어집니다.

---

### ③ PPT 및 문서 자동 생성

승인된 Plan을 기반으로 다음과 같은 산출물을 생성할 수 있습니다.

- PPTX 제안서
- Markdown 문서
- HTML 문서
- DOCX 문서
- 개발설계서 형태의 보고서

LLM은 내용을 문서 구조로 변환하는 역할을 담당하고, 실제 파일 조립과 레이아웃 배치, 버전 채번은 Python 기반의 결정론적 빌더가 담당합니다.

이를 통해 같은 Plan에서 생성되는 산출물의 일관성을 높이고, AI가 PPT나 DOCX 파일을 직접 임의로 조작하는 문제를 줄였습니다.

---

### ④ 문서 버전 관리

기존 산출물을 덮어쓰지 않고 버전별로 관리합니다.

예시:

```text
제안서_v01.pptx
제안서_v02.pptx
개발설계서_v01.docx
개발설계서_v02.docx
```

Plan을 수정하고 다시 승인하면 새로운 세대가 생성되며, 산출물도 새로운 버전으로 생성됩니다.

따라서 다음과 같은 추적이 가능합니다.

```text
인터뷰 사실
   ↓
Plan 버전
   ↓
파생 문서
   ↓
최종 산출물 버전
```

---

### ⑤ 자동 검수 및 수치 무결성 확인

생성된 문서는 결정론 검수와 LLM 기반 내용 검수를 통해 확인합니다.

#### 결정론 검수

- Plan과 산출물의 수치 비교
- 표 및 차트 데이터 확인
- `(미확정)` 표시 유실 여부 확인
- 문서 구조 확인
- 문서 태그 확인
- Plan 세대와 산출물의 대응관계 확인
- 목차 번호 및 슬라이드 구조 확인

#### LLM 내용 검수

- Plan과 확정 사실의 내용 비교
- 사실과 다른 표현 확인
- 보완이 필요한 내용 확인
- 문장 및 구성 개선사항 제안

검수 결과는 다음과 같이 심각도별로 표시됩니다.

- 🔴 사실 오류, 수치 불일치
- 🟡 표현 및 구조 개선
- ⚪ 선택적 개선사항

---

## 4. 실제 활용 과정

현재 다음과 같은 흐름으로 활용할 수 있도록 구성했습니다.

### ① 프로젝트 생성

프로젝트 제목과 식별자를 입력해 새로운 작업 공간을 만듭니다.

### ② 기존 자료 등록

기존 기획자료나 참고자료를 등록합니다.

지원 형식:

- Markdown
- Text
- JSON
- CSV

### ③ Agent 인터뷰 진행

Agent가 자료를 참고해 예상되는 내용과 확인이 필요한 사항을 질문합니다.

사용자는 자유롭게 답변하거나 선택지를 선택할 수 있습니다.

### ④ 확정 사실 확인

라운드가 종료되면 Agent가 이해한 사실을 요약합니다. 사용자가 확인한 내용만 확정 사실로 저장됩니다.

### ⑤ Plan 검토 및 승인

인터뷰 결과를 바탕으로 생성된 Plan을 확인하고 필요한 부분을 수정합니다. 사용자가 승인한 Plan만 문서 생성에 사용됩니다.

### ⑥ 산출물 생성

제안서, 개발설계서 등 필요한 문서 종류를 선택하면 PPTX, Markdown, HTML, DOCX 등의 형식으로 산출물을 생성합니다.

### ⑦ 검수 및 재생성

생성된 문서를 검수하고, 수정이 필요한 경우 Plan을 수정한 후 새로운 버전의 산출물을 다시 생성합니다.

---

## 5. 적용 결과 및 개선 효과

`PlanForge`를 통해 문서 작성 업무를 다음과 같이 개선할 수 있습니다.

### 문서 작성 과정의 표준화

개인의 경험이나 작성 방식에 따라 달라지던 문서 작성 과정을 다음과 같이 표준화할 수 있습니다.

```text
자료 등록
→ 인터뷰
→ 사실 확정
→ Plan 승인
→ 산출물 생성
→ 검수
```

### 누락 정보 확인

Agent가 먼저 질문을 제시하기 때문에 문서 작성 전에 빠진 요구사항이나 확인이 필요한 정보를 파악할 수 있습니다.

### 반복 작업 감소

하나의 Plan을 기준으로 PPT, Markdown, HTML, DOCX 등 여러 형식의 문서를 생성할 수 있어 동일한 내용을 반복해서 작성하는 작업을 줄일 수 있습니다.

### 사실 및 수치 오류 방지

Plan에 포함된 수치와 산출물의 수치를 비교하고, 근거가 확인되지 않은 내용은 `(미확정)`으로 표시하여 잘못된 정보가 문서에 반영되는 것을 줄일 수 있습니다.

### 변경 이력 추적

Plan 세대와 산출물 버전을 분리해 관리하므로, 어떤 내용이 어떤 버전의 문서에서 생성되었는지 추적할 수 있습니다.

### 문서 품질 검수 지원

문서 생성 이후 구조, 수치, 사실관계, 표현 등을 자동으로 점검할 수 있어 최종 검토 시간을 줄이는 데 도움이 됩니다.

> 실제 적용 시 절감된 작성 시간이나 검수 시간 등 정량적인 결과가 있다면 해당 수치를 추가하면 좋습니다.  
> 예: “초안 작성 시간을 기존 대비 약 ○○% 단축”, “문서 형식 변환 작업을 기존 ○시간에서 ○분으로 단축”

---

## 6. 핵심 설계 원칙

이 프로젝트에서 가장 중요하게 생각한 원칙은 **Plan을 문서 콘텐츠의 유일한 원본으로 관리하는 것**입니다.

### Plan 중심의 단일 원본 관리

LLM이 생성한 PPT나 DOCX 파일을 직접 수정하지 않습니다.

문서 내용에 수정이 필요한 경우 다음 절차를 따릅니다.

```text
Plan 수정
→ 새 Plan 세대 생성
→ 승인
→ 산출물 재생성
→ 버전 증가
```

이를 통해 문서 원본과 결과물의 연결관계를 유지할 수 있습니다.

### LLM과 코드의 역할 분리

- LLM: 인터뷰, 내용 정리, 문서 구조 변환, 검수 의견 생성
- Python 코드: 파일 조립, 레이아웃 배치, 버전 채번, 수치 검증, 구조 검증

AI가 모든 것을 자유롭게 처리하도록 하기보다는, 오류가 발생하면 안 되는 부분은 코드가 통제하도록 구성했습니다.

### 기존 산출물 덮어쓰기 방지

새로운 산출물은 항상 새로운 버전으로 생성하며, 기존 파일은 보존합니다.

이를 통해 이전 결과와 현재 결과를 비교할 수 있고, 필요하면 과거 버전으로 되돌아갈 수 있습니다.

---

## 7. 향후 개선 방향

향후에는 다음과 같은 기능을 추가로 개선할 예정입니다.

- 회사 표준 문서 양식 및 PPT 템플릿 지원
- 업무 유형별 인터뷰 질문 템플릿 제공
- 제안서, 개발설계서, 회의록 등 문서 유형 확대
- 사내 문서 및 지식 검색 기능 연계
- 사용자별 또는 조직별 권한 관리
- 문서 생성 과정의 진행률 및 예상 소요시간 개선
- 검수 결과를 반영한 Plan 자동 수정 고도화
- 산출물 품질 평가 기준 및 통계 기능 추가
- 사내 AI 또는 보안 환경에 맞춘 LLM 연동 확대

---

## 8. 보안 및 데이터 활용 시 유의사항

실제 고객사 자료나 프로젝트 자료를 사용할 때는 회사 및 고객사의 보안정책을 우선 준수해야 합니다.

공유 가능한 사례를 작성할 때에는 다음 정보를 제외해야 합니다.

- 고객사명
- 프로젝트명
- 개인정보
- 실제 고객 데이터
- 계약 및 보안 자료
- 외부 공개가 제한된 산출물
- API Key 및 인증정보

공개가 어려운 사례의 경우 실제 자료를 첨부하지 않고 다음 내용을 중심으로 설명할 수 있습니다.

- Agent의 목적
- 업무 적용 방식
- 자동화한 과정
- 개선 효과
- 향후 발전 방향

또한 LLM을 사용하는 경우에는 API Key를 브라우저나 소스 코드에 노출하지 않고 서버 환경변수로 관리하도록 구성했습니다.

---

## 9. 프로젝트 정보

- **프로젝트명:** PlanForge
- **분야:** 업무 AI
- **주요 기술:** FastAPI, React, TypeScript, Python, SQLite, Docker
- **LLM 연동:** OpenAI 호환 API 방식
- **주요 산출물:** PPTX, Markdown, HTML, DOCX
- **저장소:** [https://github.com/e-kyu/makeReport](https://github.com/e-kyu/makeReport)

---

## 마무리

`PlanForge`는 단순히 문서를 대신 작성하는 도구가 아니라,

> **업무 자료를 바탕으로 필요한 내용을 질문하고, 확정된 사실을 관리하며, 승인된 기획 내용을 다양한 문서로 변환하고 검수하는 업무 지원 Agent**

를 목표로 개발했습니다.

특히 제안서나 개발설계서처럼 여러 번의 확인과 수정이 필요한 문서 업무에서 다음과 같은 효과를 기대할 수 있습니다.

- 문서 작성 과정 표준화
- 누락 정보 확인
- 반복적인 문서 변환 작업 감소
- 수치 및 사실 오류 방지
- 문서 버전과 변경 이력 관리
- 생성 문서에 대한 자동 검수

향후 회사의 표준 문서 양식과 업무 환경에 맞게 발전시킨다면, 제안서·개발설계서뿐 아니라 다양한 업무 문서 작성과 검수에도 활용할 수 있을 것으로 기대합니다.

---

### 캠페인 제출 분야

**① 업무 AI**

실제 업무에 필요한 Agent를 직접 기획·개발하고 활용한 사례에 해당합니다.

※ 저장소가 비공개인 경우, 게시글에는 접근 권한이 있는 동료만 확인할 수 있다는 점을 함께 안내하거나, 화면 캡처와 기능 설명 중심으로 공유하는 것을 권장합니다.