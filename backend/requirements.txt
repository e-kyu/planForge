# M1 코어 엔진
python-pptx>=1.0
python-docx>=1.1
openai>=1.50          # LLM provider 추상화 계층 (CLAUDE.md: openai SDK 기반만 허용)

# 테스트
pytest>=8.0

# M2 웹 백엔드
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2  # postgresql+psycopg:// 방언
sse-starlette>=2.1
httpx>=0.27           # 테스트/클라이언트
python-multipart>=0.0.9  # UploadFile (소스 업로드 API)